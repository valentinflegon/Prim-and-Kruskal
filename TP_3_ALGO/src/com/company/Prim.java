package com.company;

public class Prim {
    // verifier si le graph est connexe

    //partir d'un noeud

    //choisir son path de poid le plus faible

    //relier a sont noeud de fin

    // attention a comment on choisi les aretes
    //c'est le min des path de tout les noeuds en contact
    // tant que tout les noeuds ne sont pas tout utiliser

    // ATTENTION a pas faire un triangle car il ne faut pas que les
    //deux extremité du path soit deja dans l'arbre

    // il faudra calculer le poids




}
